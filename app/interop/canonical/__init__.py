"""Canonical interoperability schema - the shape every connector transforms into/out of, so
external systems never talk to CivicLens's internal database models directly. See
app.interop.canonical.v1 for the current version and docs/INTEROPERABILITY.md for the
external-to-canonical-to-external data flow this exists to enable."""
